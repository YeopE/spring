<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.3.xsd">

    <!--
        di05_anno 패키지를 포함한 하위 패키지의 클래스 중에서 아래 어노테이션을 갖는 클래스들을 자동으로 스프링빈 객체로 등록한다.
    -->

    <!--
    di05_anno 패키지를 포함한 하위 패키지의 클래스 중에서
    아래 어노테션을 갖는 클래스들을
    자동으로 스프링빈 객체로 등록한다.

    @Controller  : 컨트롤러 객체 생성
    @Service     : 서비스객체 생성
    @Repository  : Dao 생성
    @Component  : 위에 기능에 해당하지 않는  클래스
    -->

    <context:component-scan base-package="di05_anno"/>
</beans>